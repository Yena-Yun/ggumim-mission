import { RoomImage, Swiper } from 'components';
import { Grid } from 'common';

const ProductPost = () => {
  return (
    <Grid flex>
      <RoomImage />
      <Swiper />
    </Grid>
  );
};

export default ProductPost;
